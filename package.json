{
    "workspaces": ["apps/*", "packages/*"],
    "scripts": {
        "dev": "bun run --cwd apps/pdf-service dev & bun run --cwd apps/reviewer dev",
        "build": "bun run --cwd apps/reviewer build"
    }
}
